name: Test action

on:
  issues:
    types: [opened]
  project_card:
    types: [converted]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          name_pattern: issue-{number}
